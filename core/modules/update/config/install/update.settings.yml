check:
  disabled_extensions: false
  interval_days: 1
fetch:
  url: ''
  max_attempts: 2
  timeout: 30
notification:
  emails: {  }
  threshold: all
psa:
  endpoint: 'https://updates.drupal.org/psa.json'
  enable: true
  notify: true
  check_frequency: 43200
